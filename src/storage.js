//creating a local storage for the project so far

const Storage = (() => {
    function save(key,data){

    }
    function load(key){

    }

    return {
        save,
        load,
    };
})

export default Storage;